#version 460 core
// Refraction continuation. It runs after the other lobes because it publishes
// the refractive path roughness into the shared geometry buffer.
#define FIRST_LOBE_REFRACTION
#define FIRST_LOBE_VAL 3
#include "/lib/rt/raytrace_rgen.glsl"
